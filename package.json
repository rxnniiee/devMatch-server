{
  "name": "devmatch-server",
  "version": "0.0.1",
  "description": "backend for the devmatch application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -r dotenv/config src/%npm_package_main% dotenv_config_path=.env",
    "dev": "nodemon --signal SIGTERM --exec \"node -r dotenv/config src/%npm_package_main% dotenv_config_path=.env.dev\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RonStAnhSt/devMatch-server.git"
  },
  "author": "rxnniiee",
  "contributors": [],
  "private": true,
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql2": "^2.2.5",
    "socket.io": "^4.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
